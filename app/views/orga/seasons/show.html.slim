h1 Season #{@season.name}

.conference
  dl.dl-horizontal
    dt Year
    dd = @season.name
    dt Start Date
    dd = @season.starts_at
    dt End Date
    dd = @season.ends_at

= link_to 'Edit season', [:edit, :orga, @season], class: 'btn btn-info'
= link_to 'Back', orga_seasons_path, class: 'btn btn-default'
